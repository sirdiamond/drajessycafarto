---
import Icon from "astro-icon";

interface Pros{
    title: string;
    text: string;
}

const {title, text} = Astro.props;

---


<div class="flex flex-col ">
    <p class="uppercase text-sm text-gray-400">{title}</p>
    <p class="text-sm text-gray-100 font-semibold">{text}</p>
</div>
